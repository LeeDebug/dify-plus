## 启动命令

```bash
$ cd docker
# 启动
$ docker-compose -f docker-compose.dify-plus.yaml up -d
$ docker-compose -f docker-compose.dify-plus.yaml down
# 启动(重命名)
$ docker-compose -p dify-plus -f docker-compose.dify-plus.yaml up -d
$ docker-compose -p dify-plus -f docker-compose.dify-plus.yaml down
```
